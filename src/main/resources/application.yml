spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/dev_db
    username: postgres
    password: ctumso12
  jpa:
    show-sql: true
    properties:
      hibernate:
        format-sql: true